[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'echo STEP1 && npx prisma migrate deploy 2>&1; echo MIGRATE_EXIT=$? && echo STEP2 && node dist/index.js 2>&1; echo NODE_EXIT=$?'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
