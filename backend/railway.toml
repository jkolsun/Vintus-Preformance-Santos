[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npx prisma migrate resolve --rolled-back 20260225000000_migrate_to_six_products && npx prisma migrate deploy && node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
